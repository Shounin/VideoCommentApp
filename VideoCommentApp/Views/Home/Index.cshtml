@model IEnumerable<VideoBlogApplication.Models.Comment>

@{
    ViewBag.Title = "Index";
}

<h2>Video blog application</h2>

<iframe width="560" height="315" src="//www.youtube.com/embed/OgGbNJdMaTY" frameborder="0" allowfullscreen></iframe>

<script type="text/html" id="template">
    <li class="list-group-item">
        <p>
            <span class="glyphicon glyphicon-user"></span>
            <span class="text-primary" data-content="Username"></span>
            <span data-content="CommentText"></span>
        </p>
        <p>
            <span class="text-muted" data-content="CommentDate"></span>
            @*<a class="like-comment" data-template-bind='[{"attribute":  "data-id", "value": "ID"}]'>Like</a><span class="glyphicon glyphicon-thumbs-up"></span>*@
            <button type="button" class="like-comment btn  btn-xs" data-template-bind='[{"attribute":  "data-id", "value": "ID"}]'><span class="button-text">Like</span> <span class="glyphicon glyphicon-thumbs-up"></span></button>
        </p>
        <!--<ul class="list-unstyled">
            <li>Patrekur liked this</li>
            <li>Siggi liked this</li>
        </ul>-->
    </li>
</script>


<ul class="list-group" id="cList" style="max-width: 560px;">
    @*@foreach (var item in Model)
        {

            <li class="list-group-item">
                <p>
                    <span class="glyphicon glyphicon-user"></span>
                    <span class="text-primary">@Html.DisplayFor(modelItem => item.Username)</span>
                    <span>@Html.DisplayFor(modelItem => item.CommentText)</span>
                </p>
                <p>
                    <span class="text-muted">@item.CommentDate.ToString("dd. MMMM HH:mm") - </span>
                    <a class="like-comment" href="#">Like <span class="glyphicon glyphicon-thumbs-up"></span></a>
                </p>*@
    <!--<ul class="list-unstyled">
        <li>Patrekur liked this</li>
        <li>Siggi liked this</li>
    </ul>-->
    @*</li>
        }*@
    @*<li class="list-group-item">*@
    @*@using (Html.BeginForm())
        {
            <div class="form-horizontal">
                <label class="sr-only" for="CommentText">Comment</label>
                <textarea class="form-control" rows="3" id="CommentText" name="CommentText"></textarea>
                <button type="submit" class="btn btn-primary">Send</button>
            </div>
            @Html.ValidationSummary(false)
        }*@
    @*</li>*@
    <li class="list-group-item">
        <form>
            <label class="sr-only" for="CommentText">Comment</label>
            <textarea class="form-control" rows="3" id="Comment" name="Comment"></textarea>
            @*<input type="text" id="Comment" name="comment" />*@
            <input type="button" class="btn btn-primary" id="btn-save" value="Save" />
        </form>
    </li>
</ul>